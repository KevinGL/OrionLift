// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
	
model User {
  id          Int     @id @default(autoincrement())
  role        String
  email       String  @unique
  phone       String  @unique
  firstname   String
  lastname    String
  password    String
  sectorId    Int?  @unique
  sector      Sector?      @relation(fields: [sectorId], references: [id])
  leaderOfTeam Team? @relation("Leader")
  teamId      Int?
  team        Team?    @relation("Techs", fields: [teamId], references: [id])
  maintenances    Maintenance[]
  breakdowns      Breakdown[]
  interventions   Intervention[]
  onCalls         OnCall[]
  tokens          Token[]
}

model Device {
  id              Int     @id @default(autoincrement())
  ref             String
  createdAt       DateTime @db.Date
  address         String
  zipCode         String
  city            String
  sectorId        Int
  sector          Sector      @relation(fields: [sectorId], references: [id])
  maintenances    Maintenance[]
  breakdowns      Breakdown[]
  interventions   Intervention[]
  tokens          Token[]
}

model Sector {
  id          Int     @id @default(autoincrement())
  ref         String
  devices     Device[]
  user        User?
}

model Maintenance {
  id          Int     @id @default(autoincrement())
  deviceId    Int
  device      Device      @relation(fields: [deviceId], references: [id])
  userId      Int
  user        User      @relation(fields: [userId], references: [id])
  beginAt     DateTime
  endAt       DateTime
  phoneAt     DateTime
  phoneOK     Boolean
  phoneText   String
  briefing    String
  safetyAt    DateTime
  safety      String
  wiresAt     DateTime
  wires       String
}

model Breakdown {
  id          Int     @id @default(autoincrement())
  deviceId    Int
  device      Device      @relation(fields: [deviceId], references: [id])
  userId      Int
  user        User      @relation(fields: [userId], references: [id])
  createdAt   DateTime
  takenAt     DateTime?
  beginAt     DateTime?
  endAt       DateTime?
  type        String
  description String?
  briefing    String
  onCallId    Int?
  onCall      OnCall?    @relation(fields: [onCallId], references: [id])
}

model Intervention {
  id          Int     @id @default(autoincrement())
  deviceId    Int
  device      Device      @relation(fields: [deviceId], references: [id])
  userId      Int
  user        User      @relation(fields: [userId], references: [id])
  beginAt     DateTime
  endAt       DateTime
  briefing    String
}

model Team {
  id          Int     @id @default(autoincrement())
  leaderId    Int     @unique
  leader      User    @relation("Leader", fields: [leaderId], references: [id])
  techs       User[]  @relation("Techs")
}

model OnCall {
  id          Int     @id @default(autoincrement())
  userId      Int
  user        User      @relation(fields: [userId], references: [id])
  beginAt     DateTime @db.Date
  endAt       DateTime @db.Date
  breakdowns  Breakdown[]
}

model Token {
  id          Int     @id @default(autoincrement())
  userId      Int
  user        User      @relation(fields: [userId], references: [id])
  deviceId    Int
  device      Device      @relation(fields: [deviceId], references: [id])
  token       String
  expiresAt   DateTime
}